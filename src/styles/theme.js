import {
    colors,
    fontSize,
    fontWeight,
    lineWeight,
    shadow
} from "./tokens"

const theme = {
    colors,
    fontSize,
    fontWeight,
    lineWeight,
    shadow
}

export default theme;