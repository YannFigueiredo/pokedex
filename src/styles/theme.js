import {
    colors,
    fontSize,
    fontWeight,
    lineWeight,
    shadow,
    spacing
} from "./tokens"

const theme = {
    colors,
    fontSize,
    fontWeight,
    lineWeight,
    shadow,
    spacing
}

export default theme;